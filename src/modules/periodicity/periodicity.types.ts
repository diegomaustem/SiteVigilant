export interface InputPeriodicity {
    time: string
    status: boolean
}

export interface Periodicity {
    id: number;
    time: string
    status: boolean
    createdAt : Date;
    updatedAt: Date;
}